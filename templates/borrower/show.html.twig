{% extends 'base.html.twig' %}

{% block title %}Borrower{% endblock %}

{% block body %}
    <h1>Borrower</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrower.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ borrower.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ borrower.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ borrower.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_borrower_index') }}">back to list</a>

    <a href="{{ path('app_borrower_edit', {'id': borrower.id}) }}">edit</a>

    {{ include('borrower/_delete_form.html.twig') }}
{% endblock %}
